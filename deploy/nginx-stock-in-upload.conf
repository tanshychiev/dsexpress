# DS Express - allow Stock In batches with multiple product photos.
# This file is intended for /etc/nginx/conf.d/ and is loaded inside nginx's http{} context.
client_max_body_size 50M;
client_body_timeout 120s;
